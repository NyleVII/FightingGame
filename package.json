{
	"name": "fighting-game",
	"version": "0.0.1",
	"scripts": {
		"start": "mongod --config db/config | node server/main.js",
		"install:dev": "npm install",
		"install:prod": "npm install --production"
	},
	"dependencies": {
		"mongodb": "^2.2.31",
		"websocket": "^1.0.24"
	},
	"devDependencies": {
		"eslint": "^4.6.1"
	}
}
